# 📘 Lección: Templates y Rendering en Vue 3 (Parte 1)

------------------------------------------------------------------------

## 1️⃣ Introducción

El desarrollo de interfaces en Vue se basa en una **sintaxis
declarativa** que permite desplegar contenido dinámico a través de
estructuras llamadas **templates**.

Los templates conectan la vista con los datos reactivos mediante:

-   Interpolaciones
-   Directivas
-   Eventos

Este enfoque permite una experiencia de desarrollo:

-   Clara
-   Reactiva
-   Eficiente
-   Escalable

------------------------------------------------------------------------

# 🧩 ¿Qué son los Templates en Vue?

En Vue, los templates son estructuras declarativas contenidas dentro del
bloque:

``` vue
<template>
  <!-- estructura visual -->
</template>
```

Utilizan HTML enriquecido con expresiones de Vue.

Funcionan como una interfaz entre:

👉 Datos reactivos\
👉 DOM Virtual\
👉 DOM Real

------------------------------------------------------------------------

# ⚙️ Sistema de Rendering en Vue

## 🔄 Transformación de Templates

Vue transforma los templates en **render functions** que trabajan sobre
el **DOM virtual**.

## 🎯 Actualización Selectiva

Vue actualiza únicamente las partes necesarias del DOM.

## 🔁 Sincronización Automática

Cuando un dato cambia:

Estado reactivo → DOM Virtual → DOM Real → Interfaz actualizada

------------------------------------------------------------------------

# 🧠 Lógica Declarativa en Templates

Dentro de los templates podemos usar:

-   Condicionales
-   Iteraciones
-   Eventos
-   Enlaces dinámicos

Ejemplo funcional básico:

``` vue
<script setup>
import { ref } from 'vue'

const mensaje = ref('Hola Vue')
</script>

<template>
  <h1>{{ mensaje }}</h1>
  <button @click="mensaje = 'Mensaje actualizado'">
    Cambiar mensaje
  </button>
</template>
```

Vue actualiza automáticamente la vista cuando cambia el estado.

------------------------------------------------------------------------

# 🧱 Componentes en Templates

Los templates permiten usar componentes personalizados como etiquetas
HTML:

``` vue
<MiComponente />
```

Esto permite:

-   Modularidad
-   Reutilización
-   Separación de responsabilidades

------------------------------------------------------------------------

# ✨ Interpolaciones en Vue

Las interpolaciones usan doble llave:

``` vue
{{ expresión }}
```

Ejemplo simple:

``` vue
<script>
export default {
  data() {
    return {
      mensaje: 'Hola Mundo'
    }
  }
}
</script>

<template>
  <p>{{ mensaje }}</p>
</template>
```

Si `mensaje` cambia, la vista se actualiza automáticamente.

------------------------------------------------------------------------

## 🧮 Operaciones en Interpolaciones

Concatenación:

``` vue
{{ nombre + ' ' + apellido }}
```

Llamada a función:

``` vue
{{ obtenerSaludo() }}
```

Propiedad anidada:

``` vue
{{ usuario.datos.edad }}
```

------------------------------------------------------------------------

## ⚠️ Limitaciones

No se pueden usar estructuras como:

❌ if\
❌ for

Para eso se usan directivas como:

-   v-if
-   v-for

------------------------------------------------------------------------

# 📌 Directivas en Vue

Las directivas comienzan con `v-`.

Permiten aplicar comportamiento reactivo al DOM.

## Tipos principales

### 🏗 Directivas estructurales

-   v-if
-   v-else
-   v-show
-   v-for

### 🔗 Directivas de atributos

-   v-bind
-   v-model

### 🖱 Directivas de eventos

-   v-on

------------------------------------------------------------------------

## Ejemplos

### Enlace de atributo

``` vue
<a v-bind:href="url">Ir</a>
```

### Modelo bidireccional

``` vue
<input v-model="nombre">
```

------------------------------------------------------------------------

# 🎛 Modificadores

Se agregan con punto:

``` vue
@click.stop="manejarClick"
@submit.prevent="enviarFormulario"
```

.stop → detiene propagación\
.prevent → evita comportamiento por defecto

Otros:

-   .capture
-   .once

------------------------------------------------------------------------

# ✂ Alias Abreviados

v-bind abreviado:

``` vue
:src="imagen"
```

v-on abreviado:

``` vue
@click="accion"
```

Más concisos y comunes en producción.

------------------------------------------------------------------------

# 🔀 Renderización Condicional

## v-if

Elimina o crea el nodo del DOM.

``` vue
<p v-if="activo">Visible</p>
```

## v-else

``` vue
<p v-else>No visible</p>
```

------------------------------------------------------------------------

# 👁 v-show

No elimina el nodo. Solo cambia `display: none`.

``` vue
<p v-show="visible">Contenido</p>
```

------------------------------------------------------------------------

## 🆚 Diferencia v-if vs v-show

  v-if                          v-show
  ----------------------------- -------------------------------
  Agrega o remueve nodo         Mantiene nodo
  Más costoso si cambia mucho   Ideal para toggles frecuentes
  Libera memoria                Consume más memoria

------------------------------------------------------------------------

# 🔁 Renderización de Listas

Se usa `v-for`:

``` vue
<li v-for="item in items" :key="item.id">
  {{ item }}
</li>
```

Permite renderizar dinámicamente arrays u objetos.

------------------------------------------------------------------------

# 🏁 Conclusión Parte 1

En esta lección aprendimos:

-   Qué son los templates
-   Cómo funciona el sistema de rendering
-   Interpolaciones
-   Directivas
-   Modificadores
-   Renderización condicional
-   Renderización de listas

Dominar estos conceptos es esencial antes de avanzar hacia:

-   Slots
-   Componentes dinámicos
-   Render functions personalizadas

------------------------------------------------------------------------

📚 Lección diseñada para fundamentos sólidos en Vue 3.
